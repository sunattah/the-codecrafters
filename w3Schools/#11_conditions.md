# Conditionals
* Conditionals are used to perform different actions based on different conditions. Conditionals uses the if, else if and else statement and they work hand-in-hand with the comparison operators.

```go

if 5 > 3 {
    fmt.Println("5 is greater")
} else {
    fmt.Println("3 is greater")
}
```