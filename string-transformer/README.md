# == ABOUT THE ENGINEER ==
My name is sunday attah, i belong to the pointer squad.

***what the program does***
the porgram  is all about recovering a the processing unit which crash days ago, so for this program strings is the weapon so we use strings to recover.
so this how it goes:
>>>the program convert strings that are of lower case to upper case 
>>> convert strings that are of upper case to lowercase.
>>> convert strings with trailing spaces to snake case.
>>> convert strings with lowercase first letter to upper case first letter.
>>> convert strings with little words to lower case while converting the first letter to uppercase i.e the small words remaining unchange.
>>> convert strings to the reverse version of it.


# HOW TO OPERATE THE STRING TRANSFORMER #

The program is run like this:
```go
go run main.go
```
so if you run it a statement will pop out that  you should choose from any of the availables below so if you choose it will take you to the input your value and if you input the value it will give you the answer and still  pop up with thesame instructions except for the title case that will require you to input again, and type exit to terminate the whole process.